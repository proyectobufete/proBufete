<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8">
    <title>
        {% block title %}
        Bufete Popular
        {% endblock %}
    </title>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  </head>
  <body>
      <header>
        <nav class="navbar navbar-default">
            <div class="container-fluid">
              <!-- Brand and toggle get grouped for better mobile display -->
              <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                  <span class="sr-only">Toggle navigation</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="{{path("bufetes_index")}}">Bufete Popular</a>
              </div>

              <!-- Collect the nav links, forms, and other content for toggling -->
              <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                  {% block menu %}
                  <li class="active"><a href="#">Link <span class="sr-only">(current)</span></a></li>
                  <li><a href="#">Link</a></li>
                  {% endblock %}
                </ul>

                <ul class="nav navbar-nav navbar-right">
                  <li><a href="#">

                    </a>
                  </li>
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-cog"></span> Opciones </a>
                    <ul class="dropdown-menu">
                          <li><a href="{{path("bufetes_index")}}">Bufetes</a></li>
                          <li><a href="{{path("paises_index")}}">Paises</a></li>
                          <li><a href="{{path("departamentos_index")}}">Departamentos</a></li>
                          <li><a href="{{path("ciudad_index")}}">Ciudad</a></li>
                          <li><a href="{{path("demandados_index")}}">Demandados</a></li>
                          <li><a href="{{path("demandantes_index")}}">Demandantes</a></li>
                          <li><a href="{{path("estadosciviles_index")}}">Estados Civiles</a></li>
                          <li><a href="{{path("prestaciones_index")}}">Prestaciones</a></li>
                          <li><a href="{{path("pretenciones_index")}}">Pretenciones</a></li>
                          <li><a href="{{path("tipoasuntos_index")}}">Tipo asuntos</a></li>
                          <li><a href="{{path("tipocaso_index")}}">Tipo casos</a></li>
                          <li><a href="{{path("trabajos_index")}}">Trabajos</a></li>
                          <li><a href="{{path("tribunales_index")}}">Tribunales</a></li>
                      <li><a href="#">Ayuda</a></li>
                    </ul>
                  </li>
                </ul>
              </div><!-- /.navbar-collapse -->
            </div><!-- /.container-fluid -->
            </nav>
      </header>
      <section id="content">
          {% block body %}
              Content default
          {% endblock %}
      </section>
      <footer>
        Bufete Popular
      </footer>
  </body>
</html>
